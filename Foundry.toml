[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[default]
rpc_url = "${RPC_URL}"

# Optional chain alias for Arc Testnet
[chain.arc-testnet]
chain_id = 5042002
rpc_url = "https://rpc.testnet.arc.network"